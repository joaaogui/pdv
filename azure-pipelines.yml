# Node.js with Vue
# Build a Node.js project that uses Vue.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'npm install and build'

- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'ftp://waws-prod-sn1-093.ftp.azurewebsites.windows.net/'
    username: 'onearby-dev-pdv\$onearby-dev-pdv'
    password: 'XdkePkAc4XeqxkAABg48p7jsu4sszvlo9FBJD1lwawN3haqQqDNzjCq5qDui'
    rootDirectory: '$(Build.Repository.LocalPath)/dist'
    filePatterns: '**'
    remoteDirectory: 'site/wwwroot'
    clean: false
    cleanContents: true
    preservePaths: false
    trustSSL: true